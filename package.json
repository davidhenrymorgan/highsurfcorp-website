{
  "name": "highsurfcorp-website",
  "version": "1.0.0",
  "description": "High Surf Corp marketing website with blog and contact form",
  "scripts": {
    "build": "npm run build:admin && node generate-blog.js",
    "build:admin": "cd admin-ui && npm ci && npm run build && cd .. && rm -rf dist/admin && cp -r admin-ui/dist dist/admin",
    "optimize:images": "node scripts/optimize-images.js",
    "dev": "npm run build && npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.11.3",
    "resend": "^4.0.1",
    "sanitize-html": "^2.17.0"
  },
  "devDependencies": {
    "sharp": "^0.33.2",
    "vitest": "^4.0.16",
    "wrangler": "^4.54.0"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "static-site",
    "blog"
  ],
  "author": "High Surf Corp",
  "license": "PRIVATE"
}
